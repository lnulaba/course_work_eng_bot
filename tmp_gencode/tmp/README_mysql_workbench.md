# Інструкції для імпорту схеми в MySQL Workbench

## 🎯 Кроки для створення бази даних в MySQL Workbench:

### 1. **Відкрити MySQL Workbench**
- Запустіть MySQL Workbench
- Підключіться до вашого MySQL сервера

### 2. **Імпорт SQL файлу**
- Меню: **File** → **Open SQL Script**
- Оберіть файл: `mysql_workbench_schema.sql`
- Або просто скопіюйте весь код з файлу

### 3. **Виконати скрипт**
- Натисніть кнопку **⚡ Execute** (або Ctrl+Shift+Enter)
- Скрипт створить базу даних `english_learning_bot` та всі таблиці

### 4. **Перевірити результат**
- В **Navigator** панелі оновіть список схем (F5)
- Розгорніть `english_learning_bot` → `Tables`
- Ви побачите всі 7 таблиць

## 📊 Структура створеної бази даних:

### **7 таблиць з різними зв'язками:**

1. **`users`** - основна таблиця користувачів
2. **`user_progress`** - прогрес навчання (1:1 з users)
3. **`settings`** - налаштування (1:1 з users)  
4. **`topics`** - теми навчання (самореференція)
5. **`words`** - словник слів
6. **`questions`** - банк питань (N:1 з topics)
7. **`test_sessions`** - сесії тестування (зв'язки з users, topics)

### **Типи зв'язків:**
- **1:1** - users ↔ user_progress, users ↔ settings
- **1:N** - users → test_sessions, topics → questions
- **N:1** - questions → topics, test_sessions → users
- **Самореференція** - topics → topics (батьківські теми)

## 🔍 Перегляд зв'язків в Workbench:

### **Відкрити EER діаграму:**
1. **Database** → **Reverse Engineer**
2. Оберіть підключення та схему `english_learning_bot`
3. Натисніть **Next** → **Next** → **Execute**
4. Ви побачите візуальну схему всіх зв'язків

### **Або створити нову EER діаграму:**
1. **File** → **New Model**
2. **Database** → **Synchronize Model**
3. Оберіть схему `english_learning_bot`

## 📋 Тестові дані включені:

### **Користувачі:**
- John Doe (A2 рівень, 150 питань, 80% accuracy)
- Maria Smith (B1 рівень, 280 питань, 78.6% accuracy)  
- Alex Brown (A1 рівень, 95 питань, 73.7% accuracy)

### **Теми з ієрархією:**
- **Grammar** → Articles, Prepositions
- **Vocabulary** → Basic Vocabulary, Advanced Vocabulary
- **Tenses** → Present Tenses, Past Tenses, Future Tenses
- **Reading**, **Listening**

### **Питання та слова:**
- 5 тестових питань різних рівнів
- 5 слів з перекладами та транскрипцією
- Тестові сесії з різними типами

## 🛠️ Додаткові можливості:

### **Автоматичні тригери:**
- Розрахунок accuracy в user_progress
- Оновлення статистики сесій
- Автоматичне встановлення is_completed

### **Представлення (Views):**
- `user_full_info` - повна інформація про користувачів
- `topics_hierarchy` - ієрархія тем з статистикою
- `session_statistics` - статистика сесій користувачів

### **Функції та процедури:**
- `get_user_level_words()` - отримання слів для рівня
- `update_progress_after_session()` - оновлення прогресу

## 🔍 Приклади запитів для тестування:

```sql
-- Перевірити всі зв'язки
SELECT TABLE_NAME, COLUMN_NAME, REFERENCED_TABLE_NAME, REFERENCED_COLUMN_NAME
FROM INFORMATION_SCHEMA.KEY_COLUMN_USAGE 
WHERE TABLE_SCHEMA = 'english_learning_bot' 
  AND REFERENCED_TABLE_NAME IS NOT NULL;

-- Повна інформація про користувача
SELECT * FROM user_full_info WHERE user_id = 12345;

-- Ієрархія тем
SELECT * FROM topics_hierarchy ORDER BY COALESCE(parent_topic_id, id);

-- Статистика по користувачах
SELECT * FROM session_statistics;
```

## ⚠️ Важливі нотатки:

1. **Кодування:** База використовує `utf8mb4` для підтримки емодзі
2. **InnoDB:** Всі таблиці використовують InnoDB для підтримки foreign keys
3. **Каскадні операції:** Видалення користувача видалить пов'язані дані
4. **Індексація:** Додані індекси для оптимізації запитів
5. **Обмеження:** ENUM поля мають обмежені значення для цілісності даних

Тепер у вас є повна робоча база даних з 7 таблицями та всіма типами зв'язків! 🎉
